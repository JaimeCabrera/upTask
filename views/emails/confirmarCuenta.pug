<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    style
      include ../../public/css/email.css
  body
    h2 Confirma tu cuenta en UpTask
    p Ya esta casi inscrito, solo presiona el siguiente boton
    a(href=`${confirmarUrl}`) Confirmar Cuenta
    p Si no puedes acceder a este enlace, visita #{confirmarUrl}